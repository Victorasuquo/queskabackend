# ============================================
# SENDGRID EMAIL CONFIGURATION FOR QUESKA
# ============================================
# 
# SETUP STEPS:
# 1. Create account at https://signup.sendgrid.com/
# 2. Authenticate your domain (queska.com) in Settings → Sender Authentication
# 3. Add the CNAME records to your DNS
# 4. Create API key in Settings → API Keys
# 5. Fill in the values below and copy to your .env file
#
# ============================================

# === SendGrid API Key ===
# Create at: https://app.sendgrid.com/settings/api_keys
# Select "Full Access" or at minimum "Mail Send" access
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# === Sender Configuration ===
# These must match your verified sender in SendGrid
EMAIL_FROM_NAME=Queska
EMAIL_FROM_ADDRESS=noreply@queska.com
EMAIL_REPLY_TO=support@queska.com

# === Dynamic Template IDs (Optional) ===
# Create templates at: Email API → Dynamic Templates
# Each template ID starts with "d-"
# Leave blank to use built-in HTML templates
SENDGRID_TEMPLATE_WELCOME=
SENDGRID_TEMPLATE_PASSWORD_RESET=
SENDGRID_TEMPLATE_EMAIL_VERIFICATION=
SENDGRID_TEMPLATE_BOOKING_CONFIRMATION=
SENDGRID_TEMPLATE_EXPERIENCE_SHARED=
SENDGRID_TEMPLATE_PAYMENT_RECEIPT=
SENDGRID_TEMPLATE_NOTIFICATION=

# === SMTP Fallback (Optional) ===
# Only needed if SendGrid is down
# You can use Gmail, Outlook, or any SMTP server
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_TLS=true

# ============================================
# IMPORTANT NOTES:
# ============================================
#
# 1. DOMAIN AUTHENTICATION
#    - Add these CNAME records to your DNS:
#      - em####.queska.com → u######.wl###.sendgrid.net
#      - s1._domainkey.queska.com → s1.domainkey.u######.wl###.sendgrid.net
#      - s2._domainkey.queska.com → s2.domainkey.u######.wl###.sendgrid.net
#    - Wait for DNS propagation (5-48 hours)
#    - Verify in SendGrid dashboard
#
# 2. SENDER VERIFICATION
#    - Create single sender at Settings → Sender Authentication
#    - Verify the email address
#
# 3. FREE TIER LIMITS
#    - 100 emails/day on free tier
#    - Upgrade for higher volumes
#
# 4. TESTING
#    - Start the app and register a user
#    - Check SendGrid Activity Feed for sent emails
#    - Check spam folder if not received
#
# ============================================

